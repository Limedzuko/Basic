import discord #importing the discord module
from discord.ext import commands #importing commands

bot = commands.Bot(
command_prefix'!'
)#definiting our bot variable which we use a lot later on, and giving it its command prefix

bot.run('BOT TOKEN') 
